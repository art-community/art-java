function len{{ spaceName }}()
    if box.space.{{ spaceName }} ~= nil then
        return box.space.{{ spaceName }}:len();
    end
end;

function count{{ spaceName }}(keys)
    if box.space.{{ spaceName }} ~= nil then
        return box.space.{{ spaceName }}:count(keys);
    end
end;