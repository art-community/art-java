package ru.art.http.xml;

public interface HttpXmlMapperConstants {
    String HTTP_XML_MAPPER_SUPPORT_ONLY_XML_ENTITIES = "Mapping from HTTP request/response request to XML valid only if request could be represented as XmlEntity";
}
