export let margin = (pixels) => ({margin: pixels});
export let marginTop = (pixels) => ({marginTop: pixels});
export let marginLeft = (pixels) => ({marginLeft: pixels});
export let padding = (pixels) => ({padding: pixels});