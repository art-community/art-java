import {PLATFORM_PATH} from "./Constants";

export const PLATFORM_ROUTER = `${PLATFORM_PATH}`;
export const USER_CARD_ROUTER = `${PLATFORM_PATH}/userCard`;
export const PROJECT_MANAGEMENT_ROUTER = `${PLATFORM_PATH}/releaseManagement`;
export const CONFIGURATION_MANAGEMENT_ROUTER =`${PLATFORM_PATH}/configurationManagement`;
export const BALANCING_MANAGEMENT_ROUTER = `${PLATFORM_PATH}/balancingManagement`;
export const CLUSTER_MANAGEMENT_ROUTER = `${PLATFORM_PATH}/clusterManagement`;
export const ADMINISTRATION_ROUTER = `${PLATFORM_PATH}/administration`;
